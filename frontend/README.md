1 Open Terminal #2 (keep Terminal #1 active in the background) and step into the frontend folder:

cd frontend

2 Install the frontend node modules:

npm install

3 Launch the Next.js local development workspace:

npm run dev

4 Open the web layout:

Launch your browser and navigate to: http://localhost:3000
